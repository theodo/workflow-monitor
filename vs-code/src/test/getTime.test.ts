import * as assert from 'assert';
import * as getTime from '../getTime';
import { storeKeyNameFromField } from 'apollo-utilities';

describe("Elapsed time calculator", () => {
    xit("calculates correct time", () => {
        let store = {
            dateLastStart: 1551737834032,
            elapsedTime: 73069
        };
    });
});